[package]
name = "tpt-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Core types and utilities for TPT BioCAD"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zip = "0.6"
thiserror = "2.0"
